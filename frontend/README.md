# Locus — Frontend

Electron + React client. Builds and runs independently of `backend/` — only Node is required, and the app talks to a Gateway URL configured via environment variable rather than any backend source.

Full scaffold (Vite, React, TypeScript, Tailwind, shadcn/ui, Redux Toolkit) lands when frontend implementation begins.
