# Helm charts

One chart per backend service plus the API Gateway, deployed against the real EKS cluster provisioned under `infra/terraform`. Not needed for local development — the local stack under `infra/local` runs everything via `docker-compose` instead.
