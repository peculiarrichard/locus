spring:
  application:
    name: analytics-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/analytics_db
    username: ${DB_USERNAME:analytics_service}
    password: ${DB_PASSWORD:analytics_service_local}
  flyway:
    enabled: true

server:
  port: 8085
