package com.locus.accountability;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Entry point for the Accountability Service — manages paired and group study accountability.
@SpringBootApplication
public class AccountabilityServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(AccountabilityServiceApplication.class, args);
    }
}
