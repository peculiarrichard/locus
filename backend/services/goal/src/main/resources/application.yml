spring:
  application:
    name: goal-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/goal_db
    username: ${DB_USERNAME:goal_service}
    password: ${DB_PASSWORD:goal_service_local}
  flyway:
    enabled: true

server:
  port: 8084
